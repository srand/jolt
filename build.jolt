from jolt import Alias, BooleanParameter, IntParameter, Parameter, Task, TaskGenerator, Tools
from jolt import attributes, influence, utils
from jolt import __version__
from jolt.pkgs import fstree
from jolt.plugins import cxx, docker, git, selfdeploy

from os import getenv


fstree_version = "26.09.193"


@influence.files("{dockerfile}")
@attributes.requires("requires_push_{push}")
@git.influence("services")
class CacheImage(docker.DockerImage):
    name = "jolt/cache"
    dockerfile = "docker/Dockerfile.cache"
    push = BooleanParameter(False)
    requires = ["docker/cli"]
    requires_push_true = ["docker/login"]
    version = Parameter(__version__)
    tags = [
        "robrt/jolt-cache:{identity}",
        "robrt/jolt-cache:latest",
        "robrt/jolt-cache:{version}",
    ]


class Jolt(selfdeploy.Jolt):
    version = Parameter(__version__)
    fstree = Parameter(fstree_version)

    def publish(self, artifact, tools):
        super().publish(artifact, tools)
        with tools.cwd(tools.builddir()):
            tools.mkdir("jolt/bin")
            tools.download("https://github.com/srand/fstree/releases/download/{fstree}/fstree-darwin-x86_64", "jolt/bin/fstree-darwin-x86_64")
            tools.download("https://github.com/srand/fstree/releases/download/{fstree}/fstree-linux-x86_64", "jolt/bin/fstree-linux-x86_64")
            tools.download("https://github.com/srand/fstree/releases/download/{fstree}/fstree-windows-x86_64.exe", "jolt/bin/fstree-windows-x86_64.exe")
            tools.chmod("jolt/bin/fstree-darwin-x86_64", 0o755)
            tools.chmod("jolt/bin/fstree-linux-x86_64", 0o755)
            tools.chmod("jolt/bin/fstree-windows-x86_64.exe", 0o755)
            artifact.collect("jolt/bin/fstree-darwin-x86_64")
            artifact.collect("jolt/bin/fstree-linux-x86_64")
            artifact.collect("jolt/bin/fstree-windows-x86_64")


@influence.files("{dockerfile}")
@attributes.requires("requires_push_{push}")
class JoltImage(docker.DockerImage):
    name = "jolt/image"
    dockerfile = "docker/Dockerfile"
    push = BooleanParameter(False)
    requires = ["jolt:version={version}", "docker/cli"]
    requires_push_true = ["docker/login"]
    tags = ["robrt/jolt:latest", "robrt/jolt:{version}", "robrt/jolt:{minor_version}"]
    version = Parameter(__version__)

    @property
    def minor_version(self):
        from jolt.version_utils import version
        v = version(str(self.version))
        return f"{v.major}.{v.minor}"

    def run(self, deps, tools):
         self.context = tools.sandbox(deps["jolt:version={version}"])
         tools.copy("{dockerfile}", "{context}/docker/")
         super().run(deps, tools)


@influence.files("{dockerfile}")
@attributes.requires("requires_push_{push}")
@git.influence("jolt/plugins/jobserver_cuse.py")
class JobServerImage(docker.DockerImage):
    name = "jolt/jobserver"
    dockerfile = "docker/Dockerfile.jobserver"
    push = BooleanParameter(False)
    requires = ["docker/cli"]
    requires_push_true = ["docker/login"]
    version = Parameter(__version__)
    tags = [
        "robrt/jolt-jobserver:{identity}",
        "robrt/jolt-jobserver:latest",
        "robrt/jolt-jobserver:{version}",
    ]


@influence.files("{dockerfile}")
@attributes.requires("requires_push_{push}")
@git.influence("services")
class SchedulerImage(docker.DockerImage):
    name = "jolt/scheduler"
    dockerfile = "docker/Dockerfile.scheduler"
    push = BooleanParameter(False)
    requires = ["docker/cli"]
    requires_push_true = ["docker/login"]
    version = Parameter(__version__)
    tags = [
        "robrt/jolt-scheduler:{identity}",
        "robrt/jolt-scheduler:latest",
        "robrt/jolt-scheduler:{version}",
    ]


@influence.files("{dockerfile}")
@attributes.requires("requires_push_{push}")
@git.influence("services")
class WorkerImage(SchedulerImage):
    name = "jolt/worker"
    buildargs = [f"FSTREE_VERSION={fstree_version}"]
    dockerfile = "docker/Dockerfile.worker"
    tags = [
        "robrt/jolt-worker:{identity}",
        "robrt/jolt-worker:latest",
        "robrt/jolt-worker:{version}",
    ]


@git.influence("docker/swarm")
class Deployment(Task):
    """
    Deploys a scheduler, a cache and a number of workers locally.

    The deployment is the docker/swarm stack, overridden to use the
    freshly built images. It runs in the foreground until interrupted
    with Ctrl-C, at which point containers and volumes are removed again.

    Configure a Jolt client to use the deployment with docker/swarm/client.conf.
    """

    name = "jolt/run"

    cacheable = False
    local = True

    project = Parameter("jolt", help="Name of the Docker Compose project.")
    version = Parameter(__version__)
    workers = IntParameter(2, help="Number of workers to deploy.")

    requires = [
        "jolt/cache:version={version}",
        "jolt/scheduler:version={version}",
        "jolt/worker:version={version}",
    ]

    override = """
services:
  cache:
    image: {{ cache }}
  scheduler:
    image: {{ scheduler }}
  worker:
    image: {{ worker }}
    deploy:
      replicas: {{ workers }}
"""

    def run(self, deps, tools):
        override = tools.render(
            self.override,
            cache=str(deps["jolt/cache:version={version}"].strings.tag),
            scheduler=str(deps["jolt/scheduler:version={version}"].strings.tag),
            worker=str(deps["jolt/worker:version={version}"].strings.tag),
            workers=int(self.workers),
        )

        with tools.cwd(tools.builddir()):
            tools.write_file("override.yaml", override, expand=False)
            self.override_file = tools.expand_path("override.yaml")

        # Relative paths in the stack file are resolved from its own directory.
        with tools.cwd("docker/swarm"):
            self.info("Scheduler listening on tcp://localhost:9090 and http://localhost:8080")
            self.info("Cache listening on tcp://localhost:9091 and http://localhost:8081")
            self.info("Press Ctrl-C to stop the deployment")

            try:
                tools.run("docker compose -p {project} -f jolt.yaml -f {override_file}"
                          " up --remove-orphans cache scheduler worker")
            finally:
                with utils.delayed_interrupt():
                    tools.run("docker compose -p {project} -f jolt.yaml -f {override_file}"
                              " down --volumes", output_on_error=True)


@influence.files("**/*.py")
@git.influence("docs")
class JoltDocs(Task):
    name = "jolt/docs"

    def run(self, deps, tools):
        with tools.cwd("docs"):
            tools.run("make clean html")

    def publish(self, artifact, tools):
        with tools.cwd("docs/_build/html"):
            artifact.collect("*")


@attributes.environ("TWINE_USERNAME")
@attributes.environ("TWINE_PASSWORD")
class JoltWheel(Task):
    name = "jolt/pypi"

    release = BooleanParameter(False)
    requires = ["jolt:version={version}"]
    version = Parameter(__version__)

    def run(self, deps, tools):
        tools.run("python3 -m pip install --upgrade setuptools wheel")
        tools.run("python3 -m pip install --upgrade twine")
        self.builddir = tools.sandbox(deps["jolt:version={version}"])
        with tools.cwd(self.builddir):
            tools.run("python3 setup.py sdist bdist_wheel")
            if self.release:
                tools.run("python3 -m twine upload dist/*")

    def publish(self, artifact, tools):
        with tools.cwd(self.builddir):
             artifact.collect("dist")


@attributes.environ("TWINE_USERNAME")
@attributes.environ("TWINE_PASSWORD")
@git.influence("docker/wrapper")
@git.influence("setup.py")
class JoltWrapperWheel(Task):
    name = "jolt/docker/pypi"

    release = BooleanParameter(False)
    version = Parameter(__version__)

    def run(self, deps, tools):
        tools.run("python3 -m pip install --upgrade setuptools wheel")
        tools.run("python3 -m pip install --upgrade twine")
        with tools.cwd(tools.builddir()):
            tools.copy("{joltdir}/docker/wrapper", ".")
            tools.run("python3 setup.py sdist bdist_wheel")
            if self.release:
                tools.run("python3 -m twine upload dist/*")

    def publish(self, artifact, tools):
        with tools.cwd(tools.builddir()):
             artifact.collect("dist")


@git.influence("debian")
class JoltDebianPackage(docker.DockerImage):
    name = "jolt/debian"
    dockerfile = "{joltdir}/debian/Dockerfile"
    extract = True
    imagefile = None
    requires = ["jolt:version={version}"]
    version = Parameter(__version__)

    def run(self, deps, tools):
        self.context = tools.builddir()
        with tools.cwd(self.context):
            deps["jolt:version={version}"].copy("*", "jolt-{version}")
            tools.copy("{joltdir}/debian", "jolt-{version}/debian/")
            with tools.cwd("jolt-{version}/debian"):
                tools.write_file("changelog", tools.render_file("changelog"))
        super().run(deps, tools)


@git.influence("protocol")
class GenerateProtobufs(cxx.Compilation):
    name = "jolt/protobufs"
    incpaths = ["."]
    sources = [
        "jolt/common.proto",
        "jolt/plugins/remote_execution/administration.proto",
        "jolt/plugins/remote_execution/log.proto",
        "jolt/plugins/remote_execution/scheduler.proto",
        "jolt/plugins/remote_execution/worker.proto",
    ]
    protogenerator = ["python", "python_grpc"]

    def run(self, deps, tools):
        with tools.nixpkgs(nixfile="shell.nix"):
            super().run(deps, tools)

    def publish(self, artifact, tools):
        with tools.cwd(self.outdir):
            artifact.collect("**/*.py")


class Default(Alias):
    push = BooleanParameter(False)

    requires = [
        "jolt/cache:push={push}",
        "jolt/docker/pypi:release={push}",
        "jolt/image:push={push}",
        "jolt/jobserver:push={push}",
        "jolt/pypi:release={push}",
        "jolt/scheduler:push={push}",
        "jolt/worker:push={push}",
    ]


